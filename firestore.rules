/**
 * Core Philosophy: This ruleset enforces a strict security model based on user ownership and role segregation.
 * User data is private and can only be accessed by the owning user. Public content like announcements and events
 * is readable by anyone, but can only be managed by users with a 'teacher' role.
 *
 * Data Structure:
 * - /users/{userId}: Stores user profiles with roles. The document ID matches the user's auth UID.
 * - /announcements/{announcementId}: Publicly readable school announcements, writable by teachers.
 * - /events/{eventId}: Publicly readable school events, writable by teachers.
 * - /resources/{resourceId}: Publicly readable resources, writes are currently disabled.
 * - /contactMessages/{contactMessageId}: A write-only 'dropbox' for contact form submissions.
 *
 * Key Security Decisions:
 * - User Privacy & Roles: The '/users' collection is locked down. Users can only access their own profile.
 *   The `role` field within a user's profile is read-only for the user to prevent self-elevation of privileges.
 *   The directory is readable by any authenticated user.
 * - Teacher-Managed Content: Writes to '/announcements' and '/events' are restricted to authenticated users
 *   who have the 'teacher' role in their user profile and are the author of the document.
 * - Public Read Access: Most content is publicly readable to serve the school portal's purpose.
 * - Secure Dropbox: '/contactMessages' allows anyone to create messages but denies all reads, updates, or deletes.
 *
 * Denormalization for Authorization:
 * - User Role: The user's role is stored directly in `/users/{userId}/role`. This is checked in rules for other collections.
 * - Ownership: `authorId` is stored on announcements and events to enforce that only the creating teacher can modify or delete them.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ----------------------------------------------------------------------
    // Helper Functions
    // ----------------------------------------------------------------------

    /**
     * Checks if the user is authenticated.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * Checks if the currently authenticated user's UID matches the provided userId.
     */
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    /**
     * Checks if the authenticated user has the 'teacher' role.
     * It reads the user's profile from the '/users' collection.
     */
    function isTeacher() {
      return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'teacher';
    }

    // ----------------------------------------------------------------------
    // User Profile Rules (/users)
    // ----------------------------------------------------------------------
    match /users/{userId} {
      allow get: if isOwner(userId) || isSignedIn();
      // Allow list for username lookup during login.
      allow list: if true; 
      allow create: if isOwner(userId) && request.resource.data.id == request.auth.uid;
      // Prevent users from changing their own role.
      allow update: if isOwner(userId) && request.resource.data.role == resource.data.role;
      allow delete: if isOwner(userId);
    }

    // ----------------------------------------------------------------------
    // Announcements Rules (/announcements)
    // ----------------------------------------------------------------------
    match /announcements/{announcementId} {
      allow get, list: if true;
      // Allow create/update/delete only for teachers who are the author.
      allow create: if isTeacher() && request.resource.data.authorId == request.auth.uid;
      allow update, delete: if isTeacher() && resource.data.authorId == request.auth.uid;
    }

    // ----------------------------------------------------------------------
    // Events Rules (/events)
    // ----------------------------------------------------------------------
    match /events/{eventId} {
      allow get, list: if true;
      // Allow create/update/delete only for teachers who are the author.
      allow create: if isTeacher() && request.resource.data.authorId == request.auth.uid;
      allow update, delete: if isTeacher() && resource.data.authorId == request.auth.uid;
    }

    // ----------------------------------------------------------------------
    // Resources Rules (/resources)
    // ----------------------------------------------------------------------
    match /resources/{resourceId} {
      allow get, list: if true;
      allow create, update, delete: if false; 
    }

    // ----------------------------------------------------------------------
    // Contact Messages Rules (/contactMessages)
    // ----------------------------------------------------------------------
    match /contactMessages/{contactMessageId} {
      allow get, list, update, delete: if false;
      allow create: if true;
    }
  }
}

    